import { StyleSheet } from "react-native";

export default StyleSheet.create({
  view: {
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    margin: 15
  },
  send: {
    width: 100
  },
  place: {
    width: 50
  }
});
